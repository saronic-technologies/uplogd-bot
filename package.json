{
  "name": "uplogd-slack-bot",
  "version": "0.1.0",
  "description": "Slack slash command bot that opens a modal and submits form data to a webhook.",
  "type": "module",
  "main": "src/app.js",
  "scripts": {
    "dev": "nodemon --watch src src/app.js",
    "start": "node src/app.js",
    "deploy": "npx pm2 start src/app.js --name uplogd-bot --watch --ignore-watch node_modules"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.2",
    "axios": "^1.6.7",
    "dotenv": "^16.4.5",
    "pm2": "^6.0.13"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
